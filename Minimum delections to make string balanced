class Solution:
    def minimumDeletions(self, s: str) -> int:
        count_b = 0
        ans = 0
        for ch in s:
            if ch == 'b':
                count_b += 1
            else:  
                ans = min(ans + 1, count_b)
        return ans
